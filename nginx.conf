events {}

http {
  server {
    listen 80;
    root /app/build;
    index index.html;

    location / {
      try_files $uri /index.html;
    }
    location /media/ {
      alias /app/media/;
    }
  }
}